#!/usr/bin/env python3
"""llm-relay dev shim — runs the CLI without installing the package."""
import os
import sys

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(os.path.abspath(__file__)), "..")))
from llm_relay.cli import main

if __name__ == "__main__":
    sys.exit(main())
